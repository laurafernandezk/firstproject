import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: "#CDC9B7",
    },
    messageContainer:{
      alignItems: "center",
      marginTop: 20
    }
    
  })